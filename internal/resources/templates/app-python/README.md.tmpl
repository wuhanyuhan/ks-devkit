# {{.Name}}

Keystone **app**（外部 MCP service）。由 `ks init --type=app --lang=python` 生成。

## 本地开发

```bash
ks dev                                              # 拉起本地 keystone（:9988）
pip install -r requirements.txt && python main.py   # app 跑在 :8080
ks register                                         # 把 manifest 注册进本地 keystone（能力网格联调）
ks refresh-meta                                     # 改 manifest/代码后重新同步
```

能力如何写、app/squad/agent/skill 怎么选：见 [decision-guide](https://github.com/wuhanyuhan/ks-devkit/blob/master/docs/decision-guide.md) 与 [best-practices](https://github.com/wuhanyuhan/ks-devkit/blob/master/docs/best-practices.md)。
